"""Config loading from configs/*.yaml + environment.

Thresholds themselves live in the YAML files, never inline (AGENTS.md 3.3).
Each module owns its own YAML section; this only loads and validates.

Owner: B. Karthikeya (shared utilities)
Scope: scope/SCOPE_B_Karthikeya.md
"""
